{
  "name": "meteor-voting-app",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "build": "meteor build .build --server localhost:3000"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  }
}
